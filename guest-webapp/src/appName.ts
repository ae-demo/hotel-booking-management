// The wireframes' navbar brand — the one place this app's display name lives,
// so App.tsx and any page that needs it import from here rather than a second
// hardcoded copy.
export const APP_NAME = "Hotel Booking";
